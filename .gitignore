# 依赖
node_modules/

# 构建产物
dist/
.vite/
*.vox
# 例外: CLI 测试依赖此样本文件, 必须纳入版本控制 (否则 CI 上 ENOENT)
!packages/core/sample.vox

# 日志
*.log
npm-debug.log*

# 系统文件
.DS_Store
Thumbs.db

# 环境变量
.env
.env.local
.env.*.local

# 文档站点构建产物
docs/.vitepress/dist/
docs/.vitepress/cache/

# 覆盖率
coverage/

# 编辑器
.idea/
.vscode/
*.swp

# 本地项目记忆 (WorkBuddy, 不入库); 但项目级 skill 需随仓库分发, 故放行 skills/
.workbuddy/*
!.workbuddy/skills/

# 临时测试产物 (core 包本地验证残留)
packages/core/tmp/

# npm 发布相关配置
# - 本地发布前请先 `npm login` (或 `npm adduser`)
# - CI 自动发布通过环境变量 NPM_TOKEN 注入 (见 .github/workflows/publish.yml)
# - scoped 包首次发布需 public (各包 package.json 已设 publishConfig.access=public)
registry=https://registry.npmjs.org/
